# 문자열 덧셈 계산기

## 구현할 기능 목록

- 콘솔에서 문자열을 입력받는다 (Console.readLine() 사용)
- 빈 문자열 입력 시 0을 반환한다
- 쉼표(,) 또는 콜론(:)을 구분자로 사용한다
- 커스텀 구분자(`//<구분자>\n`) 형식을 지원한다
- 분리된 문자열을 모두 숫자로 변환해 합산한다
- 음수 또는 잘못된 값 입력 시 IllegalArgumentException 발생
- 합산 결과를 "결과 : <숫자>" 형식으로 출력한다